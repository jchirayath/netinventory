# ── Network data — NEVER commit (contains real MACs / IPs / SSIDs) ──
devices.csv
report.md
report-by-device.csv
inventory.xlsx
wifi_scan.csv
cloud_devices.csv
events.csv
home_ssids.txt

# Cached/scraped pages and any raw captures
*.html
router_devices_raw.html

# Cached gateway session + lockout-cooldown state
router_cookies.txt
.router_cooldown

# Credentials / local config (secrets live in the macOS Keychain; never in files)
.netinv.env
*.env
secrets*

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
